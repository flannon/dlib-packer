{
  "_readme": [
    "Required environment variables: ",
    "AMI_INSTANCE_PROFILE",
    "AWS_ACCESS_KEY_ID",
    "AWS_SECRET_ACCESS_KEY",
    "BUILD_SECURITY_GROUP",
    "BUILD_SUBNET_IS",
    "BUILD_VPC_ID",
    ""
  ],
  "builders": [
    {
      "type": "amazon-ebs",
      "ami_description": "{{user `ami_description`}} AMI",
      "ami_name": "{{user `ami_nam3`}} {{timestamp}}",
      "ami_regions": [
        "us-east-1"
      ],
      "instance_type": "t2.micro",

      "ami_virtualization_type": "{{ user `ami_virtualization_type` }}",
      "iam_instance_profile": "{{ user `ami_instance_profile` }}",
      "instance_type": "{{ user `instance_type` }}",
      "region": "{{ user `region` }}",
      "region": "us-east-1",
      "ssh_username": "{{ user `ec2_username` }}",
      "vpc_id": "{{ user `vpc_id` }}",
      "subnet_id": "{{ user `subnet_id` }}",
      "security_group_ids": [ "{{ user `security_group_ids` }}" ],

      "run_tags": {
        "ami-create": "{{user `ami_name`}}"
      },
      "source_ami": "{{user `ami_base`}}",
      "ssh_username": "{{user `ami_user`}}",
      "tags": {
        "OS_Name": "{{user `ami_user`}}",
        "OS_Version": "7",
        "SHA": "{{user `ami_shaw`}}",
        "AMI": "{{user `ami_description`}}"
      }
    }
  ],
  "post-processors": [
    {
      "output": "manifest-base.json",
      "strip_path": true,
      "type": "manifest"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 1; done"
      ]
    },
    {
      "type": "shell",
      "execute_command": "echo {{ user `ec2_user` }} | {{ .Vars }} sudo -E -S bash '{{ .Path }}'",
      "scripts": [
        "./base/bin/baseline.sh",
        "./base/bin/cleanup.sh",
        "./base/bin/debug.sh"
      ]
    }
  ],
  "variables": {
    "ami_name": "baseline-{{env `AMI_SSH_USER`}}-7",
    "ami_description": "{{env `BASE_NAME`}}",
    "ami_base": "{{env `BASE_AMI_ID`}}",
    "ami_sha":  "{{env `SHA`}}",
    "ami_user":  "{{env `AMI_SSH_USER`}}",
    "ec2_user":  "{{env `AMI_SSH_USER`}}",
    "ami_virtualization_type": "hvm",
    "ami_instance_profile": "{{env `AMI_INSTANCE_PROFILE`}}",
    "instance_type": "t2.medium",
    "region": "us-east-1",
    "vpc_id": "{{env `BUILD_VPC_ID`}}",
    "subnet_id": "{{env `BUILD_SUBNET_ID`}}",
    "security_group_ids": "{{env `BUILD_SECURITY_GROUP`}}"
  }
}
